class ChatService:
	def answer(self, question: str, context: str) -> str:
		return (
			"The workflow is now wired end to end. "
			"Upload a CSV, run the pipeline, and review the bronze, silver, gold, and report outputs."
		)
